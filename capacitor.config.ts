import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.courtsplit.app",
  appName: "CourtSplit",
  webDir: "dist/public",
  android: {
    backgroundColor: "#ffffff",
  },
};

export default config;
